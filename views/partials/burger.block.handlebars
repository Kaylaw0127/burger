<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}YUMMMM!{{else}}READY TO EAT!{{/if}}
	</button>

	<button class="delete-devoured" data-id="{{this.id}}">DELETE!</button>
</li>
